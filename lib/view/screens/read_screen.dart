import 'package:flutter/material.dart';

class ReadScreens extends StatelessWidget {
  const ReadScreens({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
