import 'package:flutter/material.dart';

/// borderRadius all 3
const circular3 = BorderRadius.all(Radius.circular(3));

/// borderRadius all 4
const circular4 = BorderRadius.all(Radius.circular(4));

/// borderRadius all 6
const circular6 = BorderRadius.all(Radius.circular(6));

/// borderRadius all 8
const circular8 = BorderRadius.all(Radius.circular(8));

/// borderRadius all 16
const circular16 = BorderRadius.all(Radius.circular(16));
